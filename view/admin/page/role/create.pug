extends ../../layout/default.pug
include ../../mixin/filterStatus.pug
include ../../mixin/search.pug
include ../../mixin/pagination.pug
include ../../mixin/OptionChangeMulti.pug
include ../../mixin/alert.pug

block main 
    +error(3000)
    if(role.permission.includes("create"))
        .card.shadow-sm
            .card-body
                form(
                    method="POST"
                    action=`${prefixAdmin}/roles/create`
                )

                    .mb-3
                        label(class="form-label") Tên nhóm quyền
                        input(
                            class="form-control"
                            type="text"
                            name="name"
                            placeholder="VD: Quản lý sản phẩm"
                            required
                        )

                    .mb-3
                        label(class="form-label") Mô tả
                        textarea(
                            class="form-control"
                            name="description"
                            rows="3"
                            placeholder="Mô tả chức năng của nhóm quyền"
                        )

                    .mt-3
                        button(
                            class="btn btn-success" 
                            type="submit"
                        ) Lưu
                        if(roleAccount.permission.includes("view"))
                            a(
                                class="btn btn-secondary ms-2" 
                                href=`${prefixAdmin}/roles`
                            ) Quay lại
        script(src="/admin/JS/product.js") 
        script(src="/admin/JS/TinyMCE.js") 